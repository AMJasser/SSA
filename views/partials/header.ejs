<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="/style.css" />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Roboto+Slab:wght@100;200;300;400;500;600;700;800;900&display=swap"
            rel="stylesheet"
        />
        <title>SSA @ GT</title>
        <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png">
    <link rel="manifest" href="/favicon/site.webmanifest">
    <link rel="mask-icon" href="/favicon/safari-pinned-tab.svg" color="#006c32">
    <link rel="shortcut icon" href="/favicon/favicon.ico">
    <meta name="msapplication-TileColor" content="#da532c">
    <meta name="msapplication-config" content="/favicon/browserconfig.xml">
    <meta name="theme-color" content="#ffffff">
    </head>
    <body>
        <header>
            <h1><img src="/SSA.png" alt="logo" /></h1>
            <nav>
                <a href="/">HOME</a>
                <a href="/about">ABOUT</a>
                <a href="/members">MEMBERSHIP</a>
                <a href="/events">EVENTS</a>
                <% if (typeof user === 'object' && user.isAdmin === true) { %>
                    <a href="/manage">MANAGE</a>
                <% } %>
                <a href="/contact">CONTACT</a>
                <% if (!(typeof user === 'object')) { %>
                    <a href="/login">LOGIN</a>
                <% } else { %>
                    <a href="/logout">LOGOUT</a>
                <% } %>
                <a id="menu-icon" onclick="rotateIcon(this)">
                    <div class="bar1"></div>
                    <div class="bar2"></div>
                    <div class="bar3"></div>
                </a>
            </nav>
        </header>
        <div id="shrink-header">
            <a href="/">HOME</a>
            <a href="/about">ABOUT</a>
            <a href="/members">MEMBERSHIP</a>
            <a href="/events">EVENTS</a>
            <% if (typeof user === 'object' && user.isAdmin === true) { %>
                <a href="/manage">MANAGE</a>
            <% } %>
            <a href="/contact">CONTACT</a>
            <% if (!(typeof user === 'object')) { %>
                <a href="/login">LOGIN</a>
            <% } else { %>
                <a href="/logout">LOGOUT</a>
            <% } %>
        </div>
        <div class="alert">
            <span class="closebtn" onclick="this.parentElement.style.transform='translateY(-62px)';">&times;</span>
        </div>
        <script>
            function flash(msg) {
                let alertEl = document.querySelector(".alert");
                alertEl.innerHTML = `<span class="closebtn" onclick="this.parentElement.style.transform='translateY(-62px)';">×</span>\n ${msg}\n`;
                alertEl.style.transform = "translateY(0)";
            }

            window.onload = function() {
                <% if (typeof msg === "string") { %>
                    flash("<%= msg %>");
                <% } %>
            }
        </script>